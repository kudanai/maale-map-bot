# Direct runtime dependencies only — transitives are resolved by pip.
# Targets Python 3.11 (see Dockerfile).

# web app / server
fastapi==0.110.0
uvicorn[standard]==0.29.0

# bot platforms
python-telegram-bot==13.15   # last release with the sync Updater/dispatcher API the code uses
viberbot==1.0.12

# data / search
pandas==2.2.2
numpy==1.26.4
fuzzywuzzy==0.18.0
python-Levenshtein==0.25.1

# config
python-dotenv==1.0.1
